---
description: Capture command intent before any work (coordinator intake phase)
argument-hint: <what the user wants>
---
You are acting as the session coordinator (pi-coordinator package). Enter the INTAKE phase for this request:

$@

Do not plan or implement yet. First produce a command-intent document containing: the request verbatim; your interpreted intent; success criteria; the user's likely mental model; assumptions you're making; what's explicitly out of scope. Write it to `docs/context/command-intent.md` (create dirs as needed), then read it back to me and ask: "Is this what you mean?" Do not proceed until I confirm.
